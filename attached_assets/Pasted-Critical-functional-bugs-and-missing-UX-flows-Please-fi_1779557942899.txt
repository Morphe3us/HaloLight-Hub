Critical functional bugs and missing UX flows.

Please fix the following issues one by one and manually verify each flow.

Issue 1 — AI chatbot still not working

The AI Assistant still does not work from the user perspective.

Expected behavior:
- User clicks a suggested prompt OR types a message
- A conversation is created
- User message appears in the chat
- Assistant response streams back
- Conversation appears in the sidebar

Current behavior:
- It still does not work reliably.

Please debug end-to-end with real verification:
- frontend click/input handler
- conversation creation API
- auth token / bearer token
- database insert
- stream endpoint
- response streaming
- error toast

Do not redesign the UI.
Fix the functional issue only.
After fixing, verify:
- suggested prompt click works
- typed message + Enter works
- send button works
- stream response appears

Issue 2 — Add Equipment flow missing / not working

On the Equipment page, when the user clicks “Add Equipment” / “Register Equipment”, a modal window must open.

The modal must allow the user to add equipment with these fields:

Required:
- Equipment name
- Product model
- Serial number
- Purchase date
- Status

Additional fields:
- Weight
- Purchase price
- Vendor / supplier name
- Warranty expiration date
- Last maintenance date
- Next maintenance date
- Maintenance notes

Expected behavior:
- Click button
- Modal opens
- User fills fields
- User clicks Save
- Equipment is created in database
- Equipment appears immediately in the equipment list
- Success toast appears
- Modal closes

Please verify the full flow manually.

Issue 3 — Consumables / Supplies management missing

On the Consumables page, order history is not enough.

The user must be able to manually add a new consumable / supply stock item from anywhere.

Add a clear button:

“Add Supply” or “New Supply”

When clicked, open a modal.

The modal must allow:

- Supply name
- Category: Paper / Ribbon / Cleaning / Accessory / Other
- SKU optional
- Compatible product model optional
- Current quantity
- Unit type: rolls / sheets / boxes / units
- Estimated daily usage
- Reorder threshold
- Last restock date
- Notes

Expected behavior:
- User clicks “Add Supply”
- Modal opens
- User fills stock data
- User saves
- New supply appears in current stock list
- Stock status is calculated automatically:
  - Healthy
  - Low stock
  - Critical / out of stock
- Success toast appears

Also keep the existing Order History section.

Important:
- Do not modify unrelated modules.
- Do not redesign the whole app.
- Keep current branding and styling.
- Fix functionality and add the missing modals only.
- Use existing API patterns.
- If API endpoints are missing, add them.
- If generated hooks are missing, update OpenAPI/codegen properly.
- Run typecheck.
- Manually verify all three flows before reporting completion.

At the end, provide:
1. What was broken
2. What was fixed
3. What files changed
4. How each flow was manually verified